name: create release

on: 
  push: 
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    # ...
    permissions:
      contents: write
    steps:
      - uses: Kir-Antipov/mc-publish@v3.3
        with:
          # Only include this section if you wish to publish
          # your assets on Modrinth.
          modrinth-id: xfcXTP3W
          modrinth-token: ${{ secrets.mrp_wcTxHTsWRFRv8fLbM63CNOehuByffxn99qJvGbigeQOsgLVHwjOYSXMWRbYf }}

          # Only include this section if you wish to publish
          # your assets on GitHub.
          github-token: ${{ secrets.GITHUB_TOKEN }}
